"""Service layer for BNG host management.

Each submodule in this package wraps a specific system subsystem
(accel-ppp, nftables, network interfaces, etc.) and exposes
operations consumed by the REST API routers.
"""
